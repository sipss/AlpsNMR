% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psalsa_gen_synthetic_1d.R
\name{gen_synthetic_1d}
\alias{gen_synthetic_1d}
\title{Generate a synthetic 1D signal with known ground truth}
\usage{
gen_synthetic_1d(
  n = 1000,
  density_q1 = 0.02,
  density_q2 = 0.02,
  density_q3 = 0.02,
  fwhm_range = c(10, 30),
  csnr = 0.03,
  peak_shape = c("lorentzian", "gaussian", "gex"),
  A = 1,
  amplitude_q1 = 0.1045 * A,
  amplitude_q2 = 0.35 * A,
  amplitude_q3 = 1.1719 * A,
  seed = 1,
  cap_density = TRUE,
  min_spacing_mult = 2
)
}
\arguments{
\item{n}{Number of points.}

\item{density_q1, density_q2, density_q3}{Peaks per point, as a Q1/median/Q3
triple (e.g. from \code{pool_signal_stats_1d()}/\code{pool_signal_stats_regions_1d()},
which pool a REAL dataset's own per-sample density into exactly this
shape) rather than one fixed value -- a single density value is drawn
from this triple EACH TIME \code{gen_synthetic_1d()} is called (see
\code{draw_from_quantiles_1d()}), so a batch of synthetic draws (one call per
seed) reproduces the real dataset's own sample-to-sample density
variation instead of every draw sharing the exact same density. The
number of peaks is then \code{max(3, round(density_draw * n))} (further
reduced by \code{cap_density}). All three equal reproduces the old
deterministic behaviour (every draw gets that one fixed density).}

\item{fwhm_range}{\code{c(lo, hi)}: each peak's full width at half maximum (in
points) is drawn uniformly from this range.}

\item{csnr}{Noise scale, roughly the noise standard deviation as a
fraction of the signal amplitude \code{A}. \code{0} gives an exactly noise-free
signal.}

\item{peak_shape}{One of \code{"lorentzian"} (the default; the peak shape
typical of NMR spectra, the same Cauchy/Lorentzian used by
\code{\link[=peaklist_fit_lorentzians]{peaklist_fit_lorentzians()}}), \code{"gaussian"}, or \code{"gex"} (an asymmetric,
exponentially-modified peak shape typical of chromatography).}

\item{A}{Amplitude scale for the smooth baseline component and the noise
level (\code{sigma <- A * csnr * ...}) -- NOT used for peak heights directly;
see \code{amplitude_q1}/\code{amplitude_q2}/\code{amplitude_q3} for those. Their
defaults are expressed in terms of \code{A} purely to keep a bare
\code{gen_synthetic_1d()} call's peak heights at the same rough scale as
before this parameter existed.}

\item{amplitude_q1, amplitude_q2, amplitude_q3}{Peak height (amplitude), as
a Q1/median/Q3 triple (e.g. from \code{summarize_peaks_1d()}'s own
\code{amplitude_q1/q2/q3}, which characterizes a REAL sample or region's own
peak height distribution). EACH peak draws its OWN height independently
from this triple (see \code{draw_from_quantiles_1d()}), instead of every peak
in a draw sharing one lognormal centered on a single \code{A}. The defaults
approximate \code{Lognormal(meanlog = log(0.35 * A), sdlog = log(6))}'s own
25th/50th/75th percentiles (this function's pre-existing peak-height
distribution when no real amplitude analysis is available), so a bare
call without real data behaves like before.}

\item{seed}{Random seed, for reproducibility.}

\item{cap_density}{If \code{TRUE} (the default), reduces the peak count so
peaks have room to stay non-overlapping (\code{density} and \code{fwhm_range}
themselves are left untouched -- only how many peaks are actually
placed). Peak centers are still placed uniformly at random, so this
lowers the odds of overlap rather than guaranteeing it; set to \code{FALSE}
to allow arbitrarily dense, overlapping peaks.}

\item{min_spacing_mult}{Only used when \code{cap_density = TRUE}: the peak
count is capped so the average spacing between peaks is at least
\code{min_spacing_mult} times the widest FWHM in \code{fwhm_range}.}
}
\value{
A list with:
\describe{
\item{\code{y}}{the synthetic signal, \code{baseline + peaks + noise}.}
\item{\code{baseline}, \code{peaks}, \code{noise}}{the individual components of \code{y}.}
\item{\code{sigma}}{the (possibly position-dependent) noise standard
deviation used to generate \code{noise}.}
\item{\code{peak_info}}{a data frame with one row per individual peak:
\code{height}, \code{fwhm}, \code{center}, the index window \code{lo}:\code{hi} where that
peak alone exceeds 0.1\% of its own height, \code{area} (its exact
true area, \code{sum()} of that peak's own values over the full signal),
and \code{a}/\code{b} (the \code{"gex"} shape parameters, \code{NA} for other shapes) --
enough to exactly reconstruct that one peak's own curve later,
without storing a dense per-peak matrix.}
\item{\code{peak_shape}}{the \code{peak_shape} used, needed alongside
\code{peak_info} to reconstruct individual peaks.}
}
}
\description{
Builds a synthetic spectrum (a smooth baseline plus a number of peaks plus
noise) with a known true baseline and known individual peak areas, useful
for testing baseline-estimation code or trying out \code{\link[=psalsa]{psalsa()}} without real
data. This is what \code{\link[=tune_psalsa]{tune_psalsa()}} uses internally to build its synthetic
tuning pool, matched to the characteristics of a real example spectrum;
called directly, it is a standalone generator.
}
\examples{
sig <- AlpsNMR:::gen_synthetic_1d(n = 500, seed = 1)
plot(sig$y, type = "l")
lines(sig$baseline, col = "red")

result <- AlpsNMR:::psalsa(sig$y, lambda = 1e6)
lines(result$baseline, col = "blue", lty = 2)

}
