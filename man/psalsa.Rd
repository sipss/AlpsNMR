% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psalsa.R
\name{psalsa}
\alias{psalsa}
\title{Baseline estimation with Peaked Signal's Asymmetric Least Squares}
\usage{
psalsa(
  spectra,
  lambda = 1e+07,
  p = 0.001,
  k = -1,
  maxit = 25,
  k_epsilon = 1e-06
)
}
\arguments{
\item{spectra}{Either a numeric vector containing one spectrum, or a matrix
containing one spectrum per row.}

\item{lambda}{Smoothing parameter (generally \code{1e5} - \code{1e8}).}

\item{p}{Asymmetry parameter.}

\item{k}{Peak height parameter, controlling how strongly the weights of
points above the baseline decay with their height. Usually about 5\% of the
maximum intensity. When \code{k = -1} (the default) it is set to one twentieth
of the maximum intensity of each spectrum.}

\item{maxit}{Maximum number of iterations.}

\item{k_epsilon}{Floors \code{k} at \code{k_epsilon * diff(range(spectra))}, so an
estimated or supplied \code{k} that is (near) zero can't make the asymmetric
weight \code{exp(-d/k)} collapse to 0 for essentially any positive residual --
which happens for a genuinely near-noise-free signal, since \code{k} is
usually derived from an estimated noise level (see \code{\link[=tune_psalsa]{tune_psalsa()}}),
silently degenerating the baseline toward the signal's minimum. Scaled by
the data's own range (rather than a fixed absolute value) so the floor
stays proportionate regardless of the signal's amplitude.}
}
\value{
A list with two elements, each with the same dimensions as
\code{spectra}:
\describe{
\item{\code{baseline}}{the estimated baseline.}
\item{\code{corrected}}{the baseline-corrected signal, i.e.
\code{spectra - baseline}.}
}
}
\description{
Estimates the baseline of one or more spectra using the PSALSA
(Peaked Signal's Asymmetric Least Squares Algorithm) method. PSALSA is a
variant of asymmetric least squares that limits the influence of intense
peaks on the estimated baseline, which improves the result for signals
containing sharp, high peaks.
}
\details{
Like ordinary asymmetric least squares, PSALSA assigns different weights to
the points above and below an iteratively estimated baseline: the asymmetry
parameter \code{p} (\verb{0 <= p <= 1}) is the weight for points below the baseline,
whereas points above it normally receive weight \code{1 - p}. The difference is
that for points above the baseline the weight decays exponentially with the
height of the point above the current estimate, controlled by \code{k}. This
prevents intense peaks from pulling the baseline upwards. The parameter
\code{lambda} controls the amount of smoothing: the larger it is, the smoother
the baseline will be. Iteration stops once the set of points above the
baseline no longer changes, or after \code{maxit} iterations.

The baseline is fitted with a weighted second-order Whittaker smoother.
}
\examples{
x <- seq_len(200)
baseline <- 10 + 5 * sin(x / 40)
peak <- 80 * exp(-((x - 120)^2) / (2 * 4^2))
y <- baseline + peak

result <- psalsa(y)
plot(y, type = "l")
lines(result$baseline, col = "red")

}
\references{
Oller-Moreno, S., Pardo, A., Jimenez-Soto, J. M., Samitier, J., Marco, S.
(2014). "Adaptive Asymmetric Least Squares baseline estimation for analytical
instruments". 2014 IEEE 11th International Multi-Conference on Systems,
Signals & Devices (SSD14), 1-5. \doi{10.1109/SSD.2014.6808837}
}
