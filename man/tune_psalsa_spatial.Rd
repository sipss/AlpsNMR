% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psalsa_tune.R
\name{tune_psalsa_spatial}
\alias{tune_psalsa_spatial}
\title{Tune position-varying PSALSA parameters from one or more example spectra}
\usage{
tune_psalsa_spatial(
  y,
  peak_shape = c("lorentzian", "gaussian", "gex"),
  n_synthetic = 10,
  optim_maxit = 150,
  optim_reltol = 1e-06,
  num_regions = 20,
  p_max = 0.05,
  region_lambda_k_weight = 2,
  region_p_weight = 10,
  min_peaks = 15
)
}
\arguments{
\item{y}{A numeric vector with one example spectrum, or a list of numeric
vectors with several example spectra (their characteristics are pooled
together before tuning). Lists of differing lengths are supported.}

\item{peak_shape}{One of \code{"lorentzian"} (the default; the peak shape
typical of NMR spectra), \code{"gaussian"}, or \code{"gex"} (an asymmetric,
exponentially-modified peak shape typical of chromatography). Peak shape
is often known from the instrument/technique used to acquire \code{y}, so it
is left as an explicit argument rather than inferred from the data.}

\item{n_synthetic}{Number of synthetic spectra generated for the search.}

\item{optim_maxit}{Maximum number of \code{\link[stats:optim]{stats::optim()}} (Nelder-Mead)
iterations.}

\item{optim_reltol}{\code{\link[stats:optim]{stats::optim()}} relative convergence tolerance.}

\item{num_regions}{Number of contiguous regions to characterize and tune
independently.}

\item{p_max}{Upper bound used for \code{p}'s logit-space spline interpolation
(see \code{spatial_profile_1d()}); should match \code{tune_psalsa_params_1d()}'s own
\code{p_max} (its default, \code{0.05}, is used here too).}

\item{region_lambda_k_weight, region_p_weight}{Ridge-regularization weights
for each region's own search toward the signal-wide anchor -- the same
role as \code{tune_psalsa_params_1d()}'s own \code{lambda_k_weight}/\code{p_weight}, but
defaulting much stronger (verified necessary; see Details) since a single
region typically has far less data to constrain the search than the
whole spectrum does.}

\item{min_peaks}{Before tuning, adjacent regions with fewer than this many
estimated peaks are merged together (see \code{merge_sparse_regions_1d()}),
so every region actually tuned has enough real peaks of its own to
constrain the search -- addressing sparse-region collapse at its root
cause (too little data) rather than relying only on
\code{region_lambda_k_weight}/\code{region_p_weight} to paper over it.}
}
\value{
If \code{y} is a single numeric vector, a list with:
\describe{
\item{\code{baseline}, \code{corrected}}{as in \code{\link[=psalsa]{psalsa()}}, for \code{y} with the tuned
position-varying parameters.}
\item{\code{lambda}, \code{p}, \code{k}}{the tuned position-varying parameter profiles,
each the same length as \code{y}, reusable via \code{psalsa(other_y, lambda = lambda, p = p, k = k)} on similarly-lengthed spectra without tuning
again (resample with \code{resample_profile_1d()} first if the length
differs).}
\item{\code{region_params}}{a data frame with the per-region tuned values
the profiles were spline-interpolated from (\code{region}, \code{frac_mid},
\code{lambda}, \code{p}, \code{k_mult}), useful for inspecting what the tuning
actually found region by region.}
\item{\code{noise_sd}}{the signal-wide noise level \code{k_mult} was scaled by
to get \code{k}. \code{region_params} + \code{noise_sd} are enough to rebuild
\code{lambda}/\code{p}/\code{k} later without re-tuning -- much smaller to store
than the profiles themselves -- via \code{psalsa_region_params_to_profile()}.}
}
If \code{y} is a list, \code{baseline} and \code{corrected} are lists (one element per
input spectrum, using the same tuned profiles -- resampled to that
spectrum's own length where needed).
}
\description{
Like \code{\link[=tune_psalsa]{tune_psalsa()}}, but instead of a single, signal-wide \code{lambda}/\code{p}/\code{k},
tunes each of \code{num_regions} regions independently (via
\code{tune_psalsa_region_params_1d()}: each region gets its own synthetic pool,
matched to that region's own peak density/width, and its own
\code{tune_psalsa_params_1d()} search) and combines the per-region optima into
smooth, position-varying \code{lambda}/\code{p}/\code{k} profiles (one value per point),
via a natural cubic spline over each parameter's own transformed scale --
see \code{spatial_profile_1d()}. \code{\link[=psalsa]{psalsa()}} is then run ONCE per spectrum with
these profiles, using the position-varying \code{diff2_penalty_weighted()}
generalization of the smoothing penalty.
}
\details{
Useful when a single, signal-wide \code{lambda}/\code{p}/\code{k} (as tuned by
\code{\link[=tune_psalsa]{tune_psalsa()}}, even with \code{num_regions} set there merely to build a more
realistic synthetic pool) still isn't enough -- e.g. because one region's
own baseline behaviour genuinely needs a different smoothing/peak-
resistance strength than another, not just a more realistic peak density
during tuning.

Each region's own search is ridge-regularized toward a shared anchor (via
\code{tune_psalsa_params_1d()}'s own regularization mechanism, at a much
stronger weight -- see \code{region_lambda_k_weight}/\code{region_p_weight})
computed from a single signal-wide tune of the WHOLE spectrum first --
"the pack" -- rather than toward \code{tune_psalsa_params_1d()}'s generic
literature-default prior. A region with few peaks of its own has a flat,
weakly-constrained objective; verified empirically that anchoring alone,
at the SAME regularization weight the whole-spectrum search uses, is not
enough to stop such a region's knot from still collapsing to the same
"disable peak protection" degenerate optimum described in
\code{tune_psalsa_params_1d()}'s own comments (\code{lambda} collapsing toward 0,
\code{k} exploding) -- a region has far less data to resist that escape hatch
than the whole spectrum does. The stronger per-region weights keep that
region close to the pack unless its own data genuinely justifies moving
away from it.

A region with no observed peaks contributes no knot (there's nothing local
to tune against); the spline fills that stretch in smoothly from its
neighbouring regions' knots instead. If fewer than 2 regions have enough
signal to tune at all, this falls back to plain, signal-wide \code{\link[=tune_psalsa]{tune_psalsa()}}.
}
\examples{
x <- seq_len(600)
baseline <- 10 + 5 * sin(x / 100)
# A crowded region (many small peaks) and a sparse region (one big peak):
peaks <- Reduce(`+`, lapply(seq(50, 250, by = 20), function(c) {
  8 * exp(-((x - c)^2) / (2 * 3^2))
})) + 60 * exp(-((x - 500)^2) / (2 * 6^2))
set.seed(1)
y <- baseline + peaks + rnorm(length(x), 0, 0.5)

result <- AlpsNMR:::tune_psalsa_spatial(y, num_regions = 6, n_synthetic = 5)
plot(y, type = "l")
lines(result$baseline, col = "red")

}
\seealso{
\code{\link[=tune_psalsa]{tune_psalsa()}}, \code{\link[=psalsa]{psalsa()}}.
}
