% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nmr_baseline_estimation.R
\name{nmr_baseline_estimation_plot}
\alias{nmr_baseline_estimation_plot}
\title{Plot the estimated baseline against the original signal}
\usage{
nmr_baseline_estimation_plot(
  nmr_dataset,
  NMRExperiment = NULL,
  chemshift_range = NULL,
  nrow = NULL,
  ncol = NULL,
  page = 1
)
}
\arguments{
\item{nmr_dataset}{An \link{nmr_dataset_1D} with a \code{data_1r_baseline} element
(i.e. after calling \code{\link[=nmr_baseline_estimation]{nmr_baseline_estimation()}}).}

\item{NMRExperiment}{A character vector with the NMRExperiments to plot.
\code{NULL} (the default) or \code{"all"} plots every sample; for a legible plot,
pass a handful of NMRExperiments.}

\item{chemshift_range}{Either a numeric vector of length 2 (a single ppm
range to plot), or a named list of such vectors to plot several regions,
one facet per region, named after the list names.}

\item{nrow, ncol}{Number of rows/columns of region facets per page. \code{NULL}
(the default) picks a snug grid for the number of regions requested:
1x\code{n} for fewer than 4 regions, 2x2 for 4, 2x3 for 5-6, and a fixed 3x3
(paginated via \code{page}) for 7 or more.}

\item{page}{Which page of region facets to plot (1-indexed). Requesting a
page beyond the number available is an error.}
}
\value{
A ggplot2 plot with one facet per \code{chemshift_range} region
(\code{ggplot2::facet_wrap()}); every requested \code{NMRExperiment} is overlaid
within each facet. The original signal is drawn as a solid line and the
estimated baseline as a dashed line, both coloured by \code{NMRExperiment}.
}
\description{
Plots a few samples' original spectra together with their estimated
baseline (see \code{\link[=nmr_baseline_estimation]{nmr_baseline_estimation()}}), so the baseline estimate can
be visually inspected against the signal it was estimated from.
}
\examples{
dataset_1D <- nmr_dataset_load(system.file("extdata", "nmr_dataset.rds", package = "AlpsNMR"))
dataset_1D <- nmr_baseline_estimation(dataset_1D, lambda = 6, p = 0.05)
nmr_baseline_estimation_plot(
    dataset_1D,
    NMRExperiment = names(dataset_1D)[1:2],
    chemshift_range = list(Region1 = c(1.2, 1.4), Region2 = c(3.4, 3.6))
)

}
\seealso{
\code{\link[=nmr_baseline_estimation]{nmr_baseline_estimation()}}

Other baseline removal functions:
\code{\link[=nmr_baseline_estimation]{nmr_baseline_estimation()}},
\code{\link[=nmr_baseline_removal]{nmr_baseline_removal()}}
}
\concept{baseline removal functions}
