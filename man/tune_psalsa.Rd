% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psalsa_tune.R
\name{tune_psalsa}
\alias{tune_psalsa}
\title{Tune PSALSA parameters from one or more example spectra}
\usage{
tune_psalsa(
  y,
  peak_shape = c("lorentzian", "gaussian", "gex"),
  n_synthetic = 10,
  optim_maxit = 150,
  optim_reltol = 1e-06,
  num_regions = NULL
)
}
\arguments{
\item{y}{A numeric vector with one example spectrum, or a list of numeric
vectors with several example spectra (their characteristics are pooled
together before tuning). Lists of differing lengths are supported.}

\item{peak_shape}{One of \code{"lorentzian"} (the default; the peak shape
typical of NMR spectra), \code{"gaussian"}, or \code{"gex"} (an asymmetric,
exponentially-modified peak shape typical of chromatography). Peak shape
is often known from the instrument/technique used to acquire \code{y}, so it
is left as an explicit argument rather than inferred from the data.}

\item{n_synthetic}{Number of synthetic spectra generated for the search.}

\item{optim_maxit}{Maximum number of \code{\link[stats:optim]{stats::optim()}} (Nelder-Mead)
iterations.}

\item{optim_reltol}{\code{\link[stats:optim]{stats::optim()}} relative convergence tolerance.}

\item{num_regions}{If set, characterizes peak density/width separately in
this many contiguous regions of \code{y} (instead of one signal-wide, pooled
density/width) and generates the synthetic tuning pool to match that
region-by-region variation -- useful when \code{y}'s peak density is
noticeably uneven across the spectrum (e.g. crowded aliphatic vs. sparse
downfield regions), since a single blended density otherwise makes every
synthetic region look equally busy. \code{lambda}/\code{p}/\code{k} are still tuned as
single, signal-wide values either way; this only changes how realistic
the synthetic pool is. \code{NULL} (the default) keeps the original
signal-wide characterization.}
}
\value{
If \code{y} is a single numeric vector, a list with:
\describe{
\item{\code{baseline}, \code{corrected}}{as in \code{\link[=psalsa]{psalsa()}}, for \code{y} with the tuned
parameters.}
\item{\code{lambda}, \code{p}, \code{k}}{the tuned parameters, reusable via
\code{psalsa(other_y, lambda = lambda, p = p, k = k)} on similar spectra
without tuning again.}
}
If \code{y} is a list, \code{baseline} and \code{corrected} are lists (one element per
input spectrum, using the same tuned \code{lambda}/\code{p}/\code{k} for all of them).
}
\description{
\code{psalsa()} needs \code{lambda}, \code{p} and \code{k} to be chosen for the data at hand, and
good values depend on the signal's noise level, peak width and peak density.
\code{tune_psalsa()} picks them automatically from example spectra, without
requiring a known baseline: it analyzes the noise level and a rough peak
width/density summary of \code{y}, generates a batch of \emph{synthetic} spectra with
matching characteristics (for which the true baseline and peak areas are
known by construction, since they were generated), and searches for the
\code{lambda}/\code{p}/\code{k} that best recovers those synthetic peaks' areas. The
resulting parameters are then applied to \code{y} to produce the returned
baseline.
}
\details{
The search balances recovery across small, medium and large peaks (by true
area) so that a few large peaks don't dominate the objective at the expense
of small ones; it falls back to a plain baseline-accuracy objective when
the synthetic batch doesn't contain enough well-separated peaks to define
those three groups (e.g. very dense or very wide peaks, which tend to
overlap).
}
\examples{
x <- seq_len(300)
baseline <- 10 + 5 * sin(x / 60)
peaks <- 60 * exp(-((x - 90)^2) / (2 * 4^2)) +
  25 * exp(-((x - 220)^2) / (2 * 6^2))
set.seed(1)
y <- baseline + peaks + rnorm(length(x), 0, 0.5)

result <- AlpsNMR:::tune_psalsa(y)
plot(y, type = "l")
lines(result$baseline, col = "red")

}
\seealso{
\code{\link[=psalsa]{psalsa()}}, the function being tuned.
}
