% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psalsa_tune.R
\name{psalsa_region_params_to_profile}
\alias{psalsa_region_params_to_profile}
\title{Rebuild position-varying PSALSA profiles from a \code{tune_psalsa_spatial()} region table}
\usage{
psalsa_region_params_to_profile(n, region_params, noise_sd, p_max = 0.05)
}
\arguments{
\item{n}{Length of the profile to build (the length of the spectra it will
be applied to via \code{psalsa()}).}

\item{region_params}{A data frame with one row per region: \code{frac_mid}
(that region's midpoint, as a fraction 0-1 of the spectrum),
\code{lambda}, \code{p}, \code{k_mult} -- exactly the \code{region_params} element of a
\code{tune_psalsa_spatial()} result.}

\item{noise_sd}{The \code{noise_sd} element of the same \code{tune_psalsa_spatial()}
result (used to turn \code{k_mult} back into an absolute \code{k}).}

\item{p_max}{Upper bound used for \code{p}'s logit-space interpolation; must
match the \code{p_max} the original \code{tune_psalsa_spatial()} call used (its
default, \code{0.05}, is used here too).}
}
\value{
A list with \code{lambda}, \code{p}, \code{k} -- numeric vectors of length \code{n},
directly reusable via \code{psalsa(y, lambda = lambda, p = p, k = k)} (or
\code{nmr_baseline_estimation()}'s \code{lambda}/\code{p}/\code{k} arguments) without
calling \code{tune_psalsa_spatial()} again.
}
\description{
\code{tune_psalsa_spatial()}'s tuned \code{lambda}/\code{p}/\code{k} profiles are as long as
the spectrum they were tuned on (impractical to store verbatim, e.g. as a
literal in a script or vignette for later reuse without re-tuning), but
they are entirely determined by its much smaller \code{region_params} table
(one row per region -- typically a few dozen at most) together with
\code{noise_sd}. This rebuilds the full-length profiles from just those two
small, easily-stored pieces, via the same spline interpolation
\code{tune_psalsa_spatial()} itself uses (see \code{spatial_profile_1d()}) --
\code{psalsa_region_params_to_profile(n, tuned$region_params, tuned$noise_sd)}
reproduces \code{list(lambda = tuned$lambda, p = tuned$p, k = tuned$k)} exactly
for \code{n} equal to the length \code{tuned} was originally computed for.
}
\examples{
# A crowded region (many small peaks) and a sparse region (one big peak),
# each with enough peaks of its own to tune independently:
n <- 800
x <- seq_len(n)
baseline <- 5 + 2 * sin(x / 200)
peak_centers <- c(seq(10, 190, by = 10), seq(210, 390, by = 10))
peaks <- Reduce(`+`, lapply(peak_centers, function(ctr) {
  15 * exp(-((x - ctr)^2) / (2 * 1.5^2))
}))
set.seed(1)
y <- baseline + peaks + rnorm(n, 0, 0.2)

tuned <- AlpsNMR:::tune_psalsa_spatial(y, num_regions = 4, n_synthetic = 5)
# tuned$region_params (a small table) and tuned$noise_sd are cheap to
# store (e.g. as literals in a script), unlike tuned$lambda/p/k directly:
profiles <- AlpsNMR:::psalsa_region_params_to_profile(length(y), tuned$region_params, tuned$noise_sd)
stopifnot(all.equal(profiles$lambda, tuned$lambda))

}
